name: Documentation CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  check-docs:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6

    - name: Validate archive/SUMMARY.md and docs
      run: make docs-build

    - name: Check for broken internal links
      run: |
        grep -oP '(?<=\]\().*?\.md(?=\))' archive/SUMMARY.md | while read -r link; do
          if [ ! -f "$link" ]; then
            echo "ERROR: Broken link in archive/SUMMARY.md -> $link"
            false
          fi
        done
